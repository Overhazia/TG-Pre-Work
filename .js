let bucketList = ['Rappel into a cave', 'Take a falconry class', 'Learn to juggle'];

console.log(bucketList);
let newYearsResolutions = ['Fix credit', 'Go camping more', 'Nail this apprenticeship'];
console.log(newYearsResolutions);